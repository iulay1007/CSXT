#include <stdio.h>
int dg1(int n){
if(n==0)
n++;
else if(n>0)
n=n*dfs(n/2);
return n; 
} 
int dd(int n){
	int num[n];
	for(int i=0;i<=n;i++)
	{
		if(i==0)
		a[i]==1;
		else a[i]=i*a[i/2];
		
		 
	}
	
	return 0;
}
